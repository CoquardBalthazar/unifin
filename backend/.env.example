DATABASE_URL=postgres://unifin:unifin@postgres:5432/unifin
# Vitest points DATABASE_URL at this one — see vitest.config.ts.
# Must be a DIFFERENT database: the suite truncates it on every run.
DATABASE_URL_TEST=postgres://unifin:unifin@postgres:5432/unifin-test
PORT=4000
JWT_SECRET=replace-with-a-long-random-string
ADMIN_EMAIL=you@example.com
ADMIN_PASSWORD_HASH=$2b$10$replace-with-real-hash
